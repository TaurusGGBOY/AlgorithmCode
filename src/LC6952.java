import java.util.List;

class Solution {
    public long countInterestingSubarrays(List<Integer> nums, int modulo, int k) {
        long[][] dp = new long[nums.size()][nums.size() / modulo + 1];

    }
}
import java.util.Arrays;

class Solution {
    public int firstDayBeenInAllRooms(int[] nextVisit) {
        // dp[i] = if(next[j]==i&&times%2==1) dp[j]+1 time++
        // dp[i] = if(times%2==0) dp[i-1]+1 time++
    }
}